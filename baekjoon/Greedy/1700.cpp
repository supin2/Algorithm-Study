// 백준 1700번 : 멀티탭 스케줄링
#include <iostream>
#include <vector>

using namespace std;

int main() {
    int N, K;
    cin >> N >> K;
    vector<int> use(N);
    vector<int> appliance(K);

    for (int i = 0; i < K; i++) {
        cin >> appliance[i];
    }

    // 너무 어렵다

    return 0;
}